/*
 * Result model
 */
define([
  // dependencies
  'Underscore',
  'Backbone'
], function (_, Backbone) {
  return Backbone.Model.extend({

    initialize: function () {
      // NOP
    }

  });
});