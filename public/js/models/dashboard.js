/*
 * Dashboard Result modle
 */

define([
  // dependencies
  'jQuery',
  'Underscore',
  'Backbone',
  'util'
], function ($, _, Backbone, util) {
  return Backbone.Model.extend({

    initialize: function (params) {
      
      console.log("dashboard model init");
    },

  });
});